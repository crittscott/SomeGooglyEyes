Covered by AgeableListResolver (ArmorStandModel extends ArmorStandArmorModel extends HumanoidModel
extends AgeableListModel). No "baby" form (the small/large armor stand toggle is a different flag, not
`young`), so the wrap fix is moot either way.
