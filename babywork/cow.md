Covered by AgeableListResolver (CowModel extends QuadrupedModel; `scaleHead=false`). Adult and baby both
handled after the head/body wrap fix. Datapack was re-authored: consolidated to a single `"any"` age entry
since the same local position is now correct for both ages (see conversation history / commit for cow.json).
