Covered by AgeableListResolver (WolfModel extends ColorableAgeableListModel extends AgeableListModel;
the Colorable wrapper only tints color, doesn't add its own wrap). Has a baby form (pup), fixed by the
head/body wrap replay. Not individually checked for the cow-style hand-compensated JSON data issue.
