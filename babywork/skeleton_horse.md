Covered by AgeableListResolver — presumed to reuse HorseModel (same family as `horse`), not independently
confirmed by reading a SkeletonHorseRenderer source. Supports a baby form (rare in normal play). If wrong,
re-check against the actual registered model class.
