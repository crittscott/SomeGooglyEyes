Covered by HierarchicalResolver (VillagerModel extends HierarchicalModel, no renderToBuffer override).
Has a baby form; since the model itself does no special baby wrap (only the general outer age scale, which
our render layer inherits automatically), there's nothing for the resolver to miss.
