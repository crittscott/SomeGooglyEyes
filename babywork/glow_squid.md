Covered by HierarchicalResolver — presumed to reuse SquidModel (same family as `squid`), not
independently confirmed by reading a GlowSquidRenderer source. No baby form. SquidModel has no
renderToBuffer override either way, so this is low-risk even if the model class guess is off.
