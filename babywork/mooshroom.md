Covered by AgeableListResolver (MushroomCow reuses CowModel, same as `cow`). Has a baby form. **Data
follow-up needed**: mooshroom.json likely has the same hand-compensated baby position cow.json had before
it was fixed (authored against the old broken resolver) — not yet individually re-verified/re-exported.
