Covered by HierarchicalResolver (FrogModel extends HierarchicalModel, no renderToBuffer override). No
baby form of its own — the tadpole stage is a separate entity type/model (TadpoleModel), not a `young`
Frog.
