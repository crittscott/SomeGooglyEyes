# SomeGooglyEyes access transformers: read-only widening so the eye layer can recover model-part names
# and attach to a named part on ANY model family (not just HierarchicalModel). None of this changes
# vanilla behavior; it only widens visibility. Read by the resolvers in client.render.resolver.
# This ForgeGradle setup applies the AT to the SRG/intermediary Forge jar, so members MUST be named by
# their SRG id (a named/parchment id silently matches nothing). The official name is noted above each.

# ModelPart.children  (private Map<String, ModelPart>, the name -> child map)
public net.minecraft.client.model.geom.ModelPart f_104213_

# AgeableListModel.headParts()  (protected Iterable<ModelPart>)
public net.minecraft.client.model.AgeableListModel m_5607_()Ljava/lang/Iterable;

# AgeableListModel.bodyParts()  (protected Iterable<ModelPart>)
public net.minecraft.client.model.AgeableListModel m_5608_()Ljava/lang/Iterable;

# AgeableListModel's baby-only scale/offset constants, applied inside renderToBuffer around headParts()/
# bodyParts() and otherwise invisible to a render layer's PoseStack. Read by AgeableListResolver so a
# young entity's attach points land where the model actually draws them.
# scaleHead (boolean)
public net.minecraft.client.model.AgeableListModel f_102007_
# babyYHeadOffset (float)
public net.minecraft.client.model.AgeableListModel f_170338_
# babyZHeadOffset (float)
public net.minecraft.client.model.AgeableListModel f_170339_
# babyHeadScale (float)
public net.minecraft.client.model.AgeableListModel f_102010_
# babyBodyScale (float)
public net.minecraft.client.model.AgeableListModel f_102011_
# bodyYOffset (float)
public net.minecraft.client.model.AgeableListModel f_102012_

# LivingEntityRenderer.layers  (protected List<RenderLayer>). Widened so the eye/picker layers can be
# INSERTED before the slime's translucent outer-cube layer instead of appended after it — appended
# layers draw after the shell, whose depth writes discard eye fragments inside it (invisible eyes on
# inner parts). Forge's addLayer() only appends, hence the direct list access.
public net.minecraft.client.renderer.entity.LivingEntityRenderer f_115291_
