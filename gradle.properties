# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

## Environment Properties

# The Minecraft version must agree with the loader versions to get valid artifacts.
minecraft_version=1.21.1
# Pinned because the rendering internals, access rules, and Mixins target this exact release.
minecraft_version_range=[1.21.1]
# Gradle's compilation and development-run dependency. Matches the minimum accepted runtime,
# so the two stay in agreement.
forge_compile_version=1.21.1-52.1.16
forge_version_range=[52.1.16,53)
# Forge's loader range uses the Forge/FML major version.
forge_loader_version_range=[52,53)
neoforge_compile_version=21.1.248
neoforge_version_range=[21.1.248,22)
# NeoForge's JavaFML loader has its own version independent of the NeoForge release number.
neoforge_loader_version_range=[1,)
# Mojang official mappings layered with Parchment. See https://parchmentmc.org/docs/getting-started
parchment_minecraft=1.21.1
parchment_version=2024.11.17

# Architectury Loom platforms built from the common module.
enabled_platforms=fabric,forge,neoforge
fabric_loader_version=0.19.3
fabric_api_version=0.116.15+1.21.1
architectury_api_version=13.0.8
architectury_version_range=[13.0.8,)
geckolib_version=4.7.4


## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=somegoogly
# The human-readable display name for the mod.
mod_name=Some Googly Eyes
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=GPL-3.0
# The mod version. See https://semver.org/
mod_version=0.8.1
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.github.crittscott.somegoogly
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=crittscott
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=A revival and enhancement of iChun's Googly Eyes.
